<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_marginBottom="10dp">

    <LinearLayout
        android:id="@+id/rowConfigItem"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/shape_config_card_blue"
        android:padding="14dp"
        android:clickable="true"
        android:focusable="true">

        <ImageView
            android:id="@+id/imgConfigLock"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginEnd="12dp"
            android:src="@drawable/ic_check_circle"
            android:contentDescription="@null" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/txtConfigName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/text_primary"
                android:textSize="14sp"
                android:textStyle="bold"
                android:maxLines="1"
                android:ellipsize="end"
                android:text="config_name" />

            <TextView
                android:id="@+id/txtConfigMeta"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textColor="@color/text_secondary"
                android:textSize="11sp"
                android:maxLines="1"
                android:ellipsize="end"
                android:text="Protected" />

        </LinearLayout>

        <!-- PLAY (بدء الاتصال بهاد الكونفيغ بلا ما نخرجو من CONFIG tab) -
             أزرق = تشغيل، أحمر = توقف أثناء المحاولة/الاتصال، أخضر = توقف
             بعد نجاح الاتصال (شوف updateActiveVisuals فـConfigFragment). -->
        <FrameLayout
            android:id="@+id/btnConfigAction"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginStart="10dp"
            android:background="@drawable/shape_config_action_screen_blue"
            android:clickable="true"
            android:focusable="true">

            <ImageView
                android:id="@+id/imgConfigAction"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_play"
                android:contentDescription="@null" />

        </FrameLayout>

    </LinearLayout>

    <!-- لوحة موسعة (Info + Edit/Share/Delete) - كتبان مكان الديالوغ
         القديم، تحت الصف نفسو، ملي المستخدم يدوس (تاب عادي أو ضغط مطول)
         على جسم الصف (ماشي على الزر ديال ★/■). -->
    <LinearLayout
        android:id="@+id/expandConfigPanel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/shape_config_card_blue"
        android:layout_marginTop="2dp"
        android:padding="14dp"
        android:visibility="gone">

        <TextView
            android:id="@+id/txtConfigInfo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/text_secondary"
            android:textSize="12sp"
            android:text="" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="10dp">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnConfigEdit"
                style="@style/Widget.MaterialComponents.Button.TextButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="EDIT"
                android:textColor="@color/text_primary"
                android:textSize="12sp"
                android:drawableTop="@drawable/ic_edit"
                android:drawableTint="@color/text_primary" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnConfigShare"
                style="@style/Widget.MaterialComponents.Button.TextButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="SHARE"
                android:textColor="@color/text_primary"
                android:textSize="12sp"
                android:drawableTop="@drawable/ic_share"
                android:drawableTint="@color/text_primary" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnConfigDelete"
                style="@style/Widget.MaterialComponents.Button.TextButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="DELETE"
                android:textColor="@color/state_error"
                android:textSize="12sp"
                android:drawableTop="@drawable/ic_delete"
                android:drawableTint="@color/state_error" />

        </LinearLayout>

    </LinearLayout>

</LinearLayout>
